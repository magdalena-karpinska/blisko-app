import { lexend } from "./fonts";

// Typography
export const typography = {
  h1: `var(--font-lexend) text-4xl font-black antialiased`, // 900 weight
  h2: `var(--font-lexend) text-3xl font-bold antialiased`, // 700 weight
  h3: `var(--font-lexend) text-2xl font-medium antialiased`, // 500 weight
  body: `var(--font-lexend) text-base font-normal antialiased`, // 400 weight
};
